export { VscodeFileSystem } from './fs';
export { VscodeConfigProvider } from './config';
export { toComponents, fromComponents, uriKey, joinUriPath, formatUri, localFsPath, uriBasename } from './uri';
